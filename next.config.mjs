/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // output: "export"
  images: {
    domains: ['localhost', 'www.raheekiq.com']
  }
};

export default nextConfig;
